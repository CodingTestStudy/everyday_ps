#include <iostream>
#include <string>
#include <vector>
using namespace std;


void merge(int left, int right) {
	int mid = (left + right) / 2;
	while (left <= mid && right > mid) {
		if(arr)
	}
}
int main() {

}