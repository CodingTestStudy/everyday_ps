#include <iostream>
#include <string>
#include <algorithm>
#include <queue>
#include <vector>

using namespace std;
int n, m;
int main() {
	cin >> n;
	while (n--) {
		cin >> m;
		for (int i = 0; i < m; i++) {
			char a, int b; cin >> a >> b;
			
		}
	}
}